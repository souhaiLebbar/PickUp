<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/menuText"
        android:orderInCategory="100"
        android:title="Create New Post"
        android:visible="true"
        app:showAsAction="always"
        />
    <item
        android:id="@+id/cancelNewPost"
        android:icon="@drawable/ic_close_black_24dp"
        android:orderInCategory="200"
        android:title="Delete Entry"
        android:visible="true"
        app:showAsAction="always" />
    <item
        android:id="@+id/confirmNewPost"
        android:icon="@drawable/ic_done_black_24dp"
        android:orderInCategory="300"
        app:showAsAction="always"
        android:title="Update Entry" />

</menu>

